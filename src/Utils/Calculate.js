const add = (first, second) => {
  return first + second;
};

const multiplay = (first, second) => {
  return first * second;
};
const divideTheFirestNumberByTheSecondNumber = (first, second) => {
  return first / second;
};

export { add, multiplay };
